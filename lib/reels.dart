import 'package:flutter/material.dart';
import './main.dart';

class usereels extends StatelessWidget {
  const usereels({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child:Text('REELS'),),
    ); 
    }
}